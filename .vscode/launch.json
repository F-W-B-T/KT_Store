{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Server",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\main.exe",
            "preLaunchTask": "build-msvc",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
